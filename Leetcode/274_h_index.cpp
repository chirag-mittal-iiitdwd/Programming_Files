#include<bits/stdc++.h>
using namespace std;

class Solution {
public:
    int hIndex(vector<int>& nums) {
        int n=nums.size(),cnt=1;
        sort(nums.begin(),nums.end());

        for(int i=n-1;i>=0;i--){
            if(n-cnt)
        }
    }
};